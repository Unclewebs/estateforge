services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: estateforge
      POSTGRES_PASSWORD: estateforge_pass
      POSTGRES_DB: estateforge
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
